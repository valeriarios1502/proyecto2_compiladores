pub fn control () void {
  var i = 0;
  var arr = new i32;
  arr[0] = 10;
  arr[1] = 20;
  arr[2] = 30;
  var len = 3;

   if (i == 0) then {
    print("cero");
    } else then {
    print("no cero");
    }

  for (arr) |elem, idx| {
    if (idx == len) then {
      break;
    }
    print(elem);
  }

  return;
}

pub fn main () void {
  control();
}